[project]
name = "sentinellite-ai"
version = "0.1.0"
description = "A lightweight Linux endpoint detection and monitoring agent with AI-assisted alert analysis."
readme = "README.md"
requires-python = ">=3.11"
authors = [
    { name = "Kavindu Kavisara" }
]

[tool.ruff]
line-length = 100

[tool.mypy]
python_version = "3.11"
ignore_missing_imports = true
